<% response = @response %>
<%= form_for(@response) do |f| %>
  <% if response.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(response.errors.count, "error") %> prohibited this reponse from being saved:</h2>

      <ul>
      <% response.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :emotion_id %><br>
    <%= f.collection_select :emotion_id, Emotion.all, :id, :emotion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
