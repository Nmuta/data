<%= form_for(@secondary) do |f| %>
  <% if @secondary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@secondary.errors.count, "error") %> prohibited this secondary from being saved:</h2>

      <ul>
      <% @secondary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :emotion_id %><br>
    <%= f.collection_select :emotion_id, Emotion.all, :id, :emotion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
